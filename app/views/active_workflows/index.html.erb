<%- model_class = ActiveWorkflow -%>
<div class="page-header">
  <h1><%=t '.title', :default => ActiveWorkflow %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:name) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @active_workflows.each do |active_workflow| %>
      <tr>
        <td><%= active_workflow.id %></td>
        <td><%= active_workflow.name %></td>
        <td><%=l active_workflow.created_at, format: :long %></td>
        <td>
          <%= link_to active_workflow_path(active_workflow), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to active_workflow_path(active_workflow), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_active_workflow_path,
            :class => 'btn btn-primary' %>

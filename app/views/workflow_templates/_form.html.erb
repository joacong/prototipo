<%= simple_form_for(@workflow_template) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :workflow_template %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

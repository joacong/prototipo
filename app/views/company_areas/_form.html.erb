<%= simple_form_for(@company_area) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :company_area %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
